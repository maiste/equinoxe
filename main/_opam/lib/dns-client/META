version = "6.1.4"
description = ""
requires = "dns dns.cache domain-name randomconv"
archive(byte) = "dns_client.cma"
archive(native) = "dns_client.cmxa"
plugin(byte) = "dns_client.cma"
plugin(native) = "dns_client.cmxs"
package "lwt" (
  directory = "lwt"
  version = "6.1.4"
  description = ""
  requires =
  "ca-certs
   dns
   dns-client
   dns-client.resolvconf
   happy-eyeballs
   ipaddr.unix
   lwt
   lwt.unix
   mirage-crypto-rng.lwt
   mtime.clock.os
   tls.lwt"
  archive(byte) = "dns_client_lwt.cma"
  archive(native) = "dns_client_lwt.cmxa"
  plugin(byte) = "dns_client_lwt.cma"
  plugin(native) = "dns_client_lwt.cmxs"
)
package "mirage" (
  directory = "mirage"
  version = "6.1.4"
  description = ""
  requires =
  "ca-certs-nss
   dns-client
   domain-name
   happy-eyeballs
   ipaddr
   mirage-clock
   mirage-random
   mirage-time
   tcpip
   tls-mirage"
  archive(byte) = "dns_client_mirage.cma"
  archive(native) = "dns_client_mirage.cmxa"
  plugin(byte) = "dns_client_mirage.cma"
  plugin(native) = "dns_client_mirage.cmxs"
)
package "resolvconf" (
  directory = "resolvconf"
  version = "6.1.4"
  description = ""
  requires = "ipaddr"
  archive(byte) = "dns_resolvconv.cma"
  archive(native) = "dns_resolvconv.cmxa"
  plugin(byte) = "dns_resolvconv.cma"
  plugin(native) = "dns_resolvconv.cmxs"
)
package "unix" (
  directory = "unix"
  version = "6.1.4"
  description = ""
  requires =
  "dns-client
   dns-client.resolvconf
   domain-name
   ipaddr
   ipaddr.unix
   mirage-crypto-rng.unix
   mtime.clock.os
   unix"
  archive(byte) = "dns_client_unix.cma"
  archive(native) = "dns_client_unix.cmxa"
  plugin(byte) = "dns_client_unix.cma"
  plugin(native) = "dns_client_unix.cmxs"
)